---
name: Select
---

import { Playground } from 'dokz'
import {
  H2,
  H3,
  Paragraph4,
  Link,
  Select,
  OptionItem,
  OptionGroup,
  CheckIcon,
} from '@finetwork/ui'
import { Fragment } from 'react'

export const styles = {
  maxWidth: '1000px',
  padding: '2em',
  height: '20em',
}

export const allOptions = [
  {
    title: 'Fruits',
    options: [
      {
        value: 'Apple',
        label: 'apple',
      },
      {
        value: 'Banana',
        label: 'banana',
      },
      {
        value: 'Blueberry',
        label: 'blueberry',
      },
    ],
  },
  {
    title: 'Vegetables',
    options: [
      {
        value: 'Aubergine',
        label: 'aubergine',
      },
      {
        value: 'Broccoli',
        label: 'broccoli',
      },
      {
        value: 'Leek',
        label: 'leek',
      },
    ],
  },
  {
    title: 'Meat',
    options: [
      {
        value: 'Beef',
        label: 'beef',
      },
      {
        value: 'Chicken',
        label: 'chicken',
      },
      {
        value: 'lamb',
        label: 'Lamb',
      },
    ],
  },
]

export const options = [
  {
    value: 'apple',
    label: 'Apple',
  },
  {
    value: 'banana',
    label: 'Banana',
  },
  {
    value: 'blueberry',
    label: 'Blueberry',
  },
  {
    value: 'orange',
    label: 'Orange',
  },
]

<div />
<div>
  <H2 font="secondary">Select</H2>
</div>

<Paragraph4>
  This component is an implementation of a custom html select
</Paragraph4>
{/* 
<Playground>
  <div style={{ padding: '2rem' }}>
    <Select defaultValue="apple">
      {allOptions.map(({ title, options }, index) => (
        <Fragment key={title}>
          <SelectGroup>
            <SelectLabel>{title}</SelectLabel>
            {options.map(({ id, name }) => (
              <SelectItem value={id} key={id}>
                <SelectItemText>{name}</SelectItemText>
                <SelectItemIndicator Icon={CheckIcon} />
              </SelectItem>
            ))}
          </SelectGroup>
          {index < allOptions.length - 1 && <SelectSeparator />}
        </Fragment>
      ))}
    </Select>
  </div>
</Playground> */}
<div>
  <H3 font="secondary">Standard</H3>
</div>

<Playground>
  <div style={styles}>
    <Select value="Elige..." label="Futas" options={options} />
  </div>
</Playground>

<div>
  <H3 font="secondary">With Title</H3>
</div>

<Playground>
  <div style={styles}>
    <Select
      value="Elige..."
      label="Futas"
      withTitle={true}
      options={allOptions}
    />
  </div>
</Playground>

<div>
  <H3 font="secondary">Disabled</H3>
</div>

<Playground>
  <div style={styles}>
    <Select value="Elige..." label="Futas" disabled options={options} />
  </div>
</Playground>

<div>
  <H3 font="secondary">Border Radius</H3>
</div>

<Playground>
  <div style={styles}>
    <Select value="Elige..." label="Futas" options={options} />
  </div>
</Playground>
