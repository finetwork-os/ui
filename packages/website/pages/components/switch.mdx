---
name: Switch
---

import { Playground } from 'dokz'
import {
  Switch,
  H4,
  Paragraph4,
  H3,
  Link,
  RocketIcon,
  ArrowRightIcon,
} from '@finetwork/ui'

export const styles = {
  maxWidth: '1000px',
  padding: '2em',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  height: '10rem',
  justifyContent: 'space-around',
}

export const horizontalStyles = {
  maxWidth: '1000px',
  padding: '2em',
  display: 'flex',
  flexDirection: 'row',
  alignItems: 'center',
  height: '10rem',
  justifyContent: 'space-around',
}

<div />
<div>
  <H3 font="secondary">Switch</H3>
</div>

<Paragraph4>
  A control that allows the user to toggle between checked and not checked. This
  component is an implementation of{' '}
  <Link
    href="https://www.radix-ui.com/docs/primitives/components/switch"
    target="_blank"
    rel="noopener noreferrer"
  >
    Switch component
  </Link>{' '}
  from radix-ui.
</Paragraph4>

<div>
  <H4 font="secondary">Size</H4>
</div>

<Paragraph4>Possible values are medium, large</Paragraph4>

<Playground>
  <div style={styles}>
    <Switch size="medium" id="chk-1" />
    <Switch size="large" id="chk-2" />
  </div>
</Playground>

<div>
  <H4 font="secondary">Kind</H4>
</div>

<Paragraph4>Possible values are primary, secondary, tertiary</Paragraph4>

<Playground>
  <div style={styles}>
    <Switch id="chk-3" kind="primary" />
    <Switch id="chk-4" kind="secondary" />
    <Switch id="chk-5" kind="tertiary" />
  </div>
</Playground>

<div>
  <H4 font="secondary">Disabled</H4>
</div>

<Playground>
  <div style={styles}>
    <Switch id="chk-30" disabled />
  </div>
</Playground>

<div>
  <H3 font="secondary">isLoading</H3>
</div>

<Playground>
  {() => {
    const App = () => {
      const [checked, setChecked] = React.useState(true)
      const [checked2, setChecked2] = React.useState(false)
      return (
        <div style={styles}>
          <Switch
            size="medium"
            id="chk-6"
            isLoading
            checked={checked}
            handleChange={setChecked}
          />
          <Switch
            size="large"
            id="chk-7"
            isLoading
            checked={checked2}
            handleChange={setChecked2}
          />
        </div>
      )
    }
    return <App />
  }}
</Playground>

<div>
  <H3 font="secondary">loadingColor</H3>
</div>

<Playground>
  {() => {
    const App = () => {
      const [checked, setChecked] = React.useState(true)
      const [checked2, setChecked2] = React.useState(false)
      return (
        <div style={styles}>
          <Switch
            size="medium"
            id="chk-8"
            isLoading
            loadingColor="black"
            checked={checked}
            handleChange={setChecked}
          />
          <Switch
            size="large"
            id="chk-9"
            isLoading
            loadingColor="#5F3DFF"
            checked={checked2}
            handleChange={setChecked2}
          />
        </div>
      )
    }
    return <App />
  }}
</Playground>

<div>
  <H3 font="secondary">loadingSpeed</H3>
</div>

<Playground>
  {() => {
    const App = () => {
      const [checked, setChecked] = React.useState(true)
      const [checked2, setChecked2] = React.useState(false)
      return (
        <div style={styles}>
          <Switch
            size="medium"
            id="chk-10"
            isLoading
            loadingSpeed={1}
            checked={checked}
            handleChange={setChecked}
          />
          <Switch
            size="large"
            id="chk-11"
            isLoading
            loadingSpeed={4}
            checked={checked2}
            handleChange={setChecked2}
          />
        </div>
      )
    }
    return <App />
  }}
</Playground>

<div>
  <H4 font="secondary">Type</H4>
</div>

<Paragraph4>Possible values are primary, secondary, tertiary</Paragraph4>

<Playground>
  <div style={styles}>
    <Switch id="chk-20" type="standard" />
    <Switch id="chk-21" type="success" />
    <Switch id="chk-23" type="warning" />
    <Switch id="chk-24" type="error" />
    <Switch id="chk-25" type="disabled" />
  </div>
</Playground>

<div>
  <H4 font="secondary">sliderColor</H4>
</div>

<Paragraph4>Color of circle inside switch</Paragraph4>

<Playground>
  <div style={styles}>
    <Switch id="chk-26" sliderColor="yellow" />
  </div>
</Playground>

<div>
  <H4 font="secondary">switchBackgroundColor</H4>
</div>

<Paragraph4>Object that needs a checked and unckecked color</Paragraph4>

<Playground>
  <div style={styles}>
    <Switch
      id="chk-27"
      switchBackgroundColor={{ unchecked: 'red', checked: 'green' }}
    />
  </div>
</Playground>

<div>
  <H4 font="secondary">checkedEnhancer and uncheckedEnhancer</H4>
</div>

<Playground>
  {() => {
    const App = () => {
      const [checked, setChecked] = React.useState(true)
      return (
        <div style={styles}>
          <Switch
            size="large"
            id="chk-28"
            checkedEnhancer={RocketIcon}
            uncheckedEnhancer={ArrowRightIcon}
            checked={checked}
            handleChange={setChecked}
          />
        </div>
      )
    }
    return <App />
  }}
</Playground>

<div>
  <H4 font="secondary">focusOutlineColor</H4>
</div>

<Paragraph4>Color of the outline when is focused</Paragraph4>

<Playground>
  <div style={styles}>
    <Switch id="chk-31" focusOutlineColor="red" />
  </div>
</Playground>
